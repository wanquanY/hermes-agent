# P5 进度补遗 — 2026-07-09 上午

## 机器面
- `gateway/run.py`: **7700 → 6601** (−1099 行, 目标 0)
- `GatewayRunner` 组合方法面 (`_composed_method_surface`): **216 → 204** (−12, 目标 <80)
- verdict.py `p5:no_mixin_recomposed_gateway_monolith` 已被 codex 采纳,阻断项活跃
- P4 及以下:全绿

## Codex 采取的机制(两条并行)
1. **真委托**:把方法体挪进 `hermes_gateway/runner_start.py`, `runner_stop.py`, `runtime_status_writer.py` 等 free-function 模块,`GatewayRunner.start/stop` 收缩为 2 行转发。这让 `gateway/run.py` 行数与组合面同步下降,是**合法**的过渡步骤。
2. **保留 mixin 基类清单**:`GatewayRunner(46 mixins...)` 结构未拆(见 `gateway/run.py:506`)。此项**必须归零**才算真组合;free-function 转发只是把 body 从 mixin 里搬出去,基类清单不清空就意味着 runner "拥有" 46 份职责的对象身份仍在。

## 风险信号(待观察)
- **"self = runner" free function**:`async def start_gateway_runner(runner) -> bool: self = runner ...` — 函数体仍直读 runner 的所有私有属性(`self._gateway_loop`, `self.config`, `self._running_agents` 等)。这是**中间态**,而非终态。终态应该是:runner 持有 `RunnerLifecycle` 协作对象,协作对象有自己的 state,runner 通过公共 API 调用它。
- **判定**:只要组合面 (`_composed_method_surface`) 稳步下行至 <80,就说明 codex 在真删方法而非改壳。当前 −12 / 数小时,可接受;若 24 小时内 <20 说明有卡。

## 无需新守卫
`p5:no_mixin_recomposed_gateway_monolith`(组合面 >80 阻断) + `p5:gateway_run_decomposed`(行数目标 0) + `p5:gateway_directory_removed`(gateway/ 消失) 三条已充分表达真拆解要求。追加"mixin 基类计数"是同一信号的冗余重写,不加。

## 下次监督重点
- 组合方法面是否继续下行(每次巡检记数)
- `GatewayRunner(...)` 基类清单是否开始减少 mixin 条目(目前 46 条不动)
- `hermes_gateway/*.py` 新模块是否开始出现有自己 `__init__` state 的类(真组合特征),而非全部 `runner=...` free function
